from django.apps import AppConfig


class MusicplayerConfig(AppConfig):
    """Django AppConfig for the `musicplayer` application.

    Provides application registration and a single place for any
    app-specific configuration or startup hooks in the future.
    """

    name = "musicplayer"
